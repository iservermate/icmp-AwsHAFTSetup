---
  - name: "Creating Routing Table id"
    hosts: local
    gather_facts: off
    tasks:
    - name: Getting RT id
      ec2_vpc_route_table_info:
        region: ca-central-1
      register: result
    - debug:
        msg: result
